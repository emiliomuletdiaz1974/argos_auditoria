# 04 · Revisión de la documentación del 2026-09-23

**Confidencialidad:** `internal` · [← índice](README.md)

**Qué revisamos:**
- los 14 ADR;
- las 26 notas de desviación;
- la bitácora completa (2 667 líneas);
- la tabla de tareas.

**Contra qué:** el código y el árbol del repositorio en `main` (commit `5abe80e`).

**Qué buscamos:** estados incoherentes, rutas o nombres que ya no existen, contradicciones entre documentos y decisiones que no quedaron escritas en ningún sitio.

## Corregido en esta revisión

| # | Qué estaba mal | Corrección |
|---|---|---|
| 1 | Cuatro notas citaban migraciones con el número previsto y no con el real: `0006_catalog`, `0008_ontology`, `0009_applicability` y `0001_nucleo` | ARG-026-028, ARG-031-033, ARG-034-040 y ARG-048-050 citan ahora `0007`, `0009`, `0010` y `0001_core` |
| 2 | Cinco notas daban a entender que la función SQL `argos.journal_append` no existe. Sí existe (`0001_core.sql`); lo que no existe es la firma del documento, y no se llama a mano | ARG-031-033, ARG-034-040, ARG-043-047, ARG-045-046 y ARG-048-050 corregidas |
| 3 | ARG-041-042 citaba `argos_challenges.library.lint`, que no existe | Ahora cita `argos_challenges.dsl.lint_challenge` |
| 4 | ADR-0007 decía «Aceptado» sin avisar de que ADR-0012 retiró su API de campañas; ARG-043-047 tampoco lo reflejaba | Cabecera de ADR-0007: «sustituido en parte por ADR-0012»; nota anotada |
| 5 | ADR-0004 seguía titulando «Decisión (recomendada)» estando aceptado (en los dos repositorios) | «Decisión» en `argos` y en `argos-web` |
| 6 | Quedaba la carpeta `argos_challenges/api/` solo con `__pycache__` (fuera de git) | Borrada |

## Abierto: necesita decisión nuestra

| # | Qué | Por qué importa | Propuesta |
|---|---|---|---|
| A1 | Las notas **ARG-062** y **ARG-067** se escribieron durante F07-05 y F07-09 con estado «aprobada», y la bitácora dice que quedaban por revisar. No consta esa revisión | La regla dice que una desviación se aprueba **antes** de aplicarse | Revisarlas: son decisiones pequeñas (fecha del veredicto en el artefacto; ReportLab por licencia). Añadir a cada una la línea de aprobación con su fecha |
| A2 | Las notas de F01–F03 constan como aprobadas «vía ADR» o «en asíncrono» durante los refinados (S1-03, F02-R, F03-R), sin una tarea DECISIÓN | Aparecen como aprobadas por nosotros sin que haya un acto de aprobación registrado | Ratificarlas en bloque (están resumidas en [02](02-desviaciones.md)) y anotar la fecha en la bitácora |
| A3 | ARG-021-023 se amplió en F03-15 (sección 2) sin volver a aprobarse | El añadido cambia reglas de escritura del grafo | Incluirla en la ratificación de A2 |
| A4 | Los puntos 6 y 7 de F05-00 se dieron por aprobados con un «estoy de acuerdo» genérico | Es una interpretación | Confirmarlos (ADR-0008 y la nota ARG-048-050) |
| A5 | `origin` es un GitHub **público**, y los ADR, las notas y este registro (confidencialidad `internal`) están versionados | Lo marcado `internal` se publica en el próximo `push` | Pasar el repositorio a privado, o sacar lo `internal` del árbol que se publica |

## Abierto: trabajo pendiente (anotado en pendientes)

| # | Qué | Dónde se resuelve |
|---|---|---|
| P1 | La sección 6 de **las 26 notas** («actualizar el documento de fase») no se ha hecho: los `.txt` de `docs/fases/` no citan sus notas ni sus ADR, y `docs/fases/LEEME.md` pide hacerlo en el mismo cambio | Tarea de documentación propia, o dentro de `F09-16` (dossier) |
| P2 | ADR-0006 pide la imagen de OPA **fijada por digest**; el compose usa la etiqueta `1.20.2`. El job editorial del CI tampoco se dispara solo con cambios en `library/`: es un paso incondicional | `F09-09` fija por digest las imágenes externas; lo de OPA entra ahí |
| P3 | La **Fase 06 no tiene tag**: F06-99 depende de F06-05 (pesos del modelo, MANUAL), y la bitácora pasó a F07-R sin explicarlo | Nada que cambiar: F06-99 espera a F06-05; queda escrito aquí y en pendientes |
| P4 | El worktree `argos/.claude/worktrees/seguridad` está desfasado: solo tiene hasta el ADR-0011 | `F09-17` integra la rama y retira el worktree |
| P5 | La **auditoría del 2026-09-18** no tiene entrada propia en la bitácora; solo está en pendientes | `F09-17` la registra al integrarla |
| P6 | `CLAUDE.md` no dice que `origin` apunta a un GitHub público (DP-12) | Añadido en esta revisión a «Convenciones de git» |

## Abierto: desviaciones de proceso (para no repetirlas)

Nada de esto rompe el producto, pero son excepciones a nuestras propias reglas y un auditor las vería:

- **TDD sin ver el fallo antes:** F05-01 (no se vio fallar), F06-01 (el analizador se escribió antes que el test) y F07-10 (el test de integración, después).
- **F05-11 se fusionó con `make check` en rojo**, contra el paso 6 del protocolo.
- **F08-03 se trabajó sobre `main` sin rama** y el paso 1 se marcó como hecho.
- **F08-09:** al detener un `make check` colgado se cortaron también procesos pytest de otra sesión; hubo aviso, pero no hay regla. Queda como regla: **filtrar los procesos por el directorio de trabajo, nunca solo por nombre**.
- **Pruebas intermitentes sin causa aislada:** el e2e de la Fase 03 (falló una vez y pasó 8 seguidas) y la reversión del sujeto sintético en el e2e de la Fase 05. Siguen en pendientes.

## Comprobado y correcto

- **Las dos copias de ADR-0004** (`argos` y `argos-web`) son idénticas.
- **Existen tal como se describen:**
  - diario: `0001_core.sql` y los vectores `journal_v1.json`;
  - traducción: las dos capas;
  - sujeto sintético: `synthetic.py` y `demo_client_actions.py`;
  - evidencia: la prueba de conformidad WORM;
  - API y consola: el contrato OpenAPI y los tipos generados;
  - modelo: el servicio `llm` y el rol `argos_ai`;
  - los objetivos del Makefile y las etapas del CI.
- **24 de 26 notas** tienen su aprobación en la bitácora; las 2 que faltan son A1.
- **Rutas de ADR-0014 que aún no existen** (`platform/`, `libs/tls`, `argos_common.dynamic_db`…): es lo esperado, porque las tareas F09 que las crean están pendientes.
