"""Pure verification core of the evidence: no database, no store, no network (ARG-069).

The public verifier imports only this package, `argos_evidence.merkle` and the pure modules of
`argos_evidence.credential`; a test keeps it that way.
"""
