"""Local AI layer of ARGOS (ARG-051…060).

An auxiliary service with four trades —classify what is ambiguous, propose challenges from legal
text, draft and assist— and two structural prohibitions that this package implements rather than
declares: it never decides conformity, and it never sees client data in the clear.
"""
